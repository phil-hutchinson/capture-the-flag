Review the current branch diff and update `README.md` if warranted.

README.md should reflect the project at a high level — what it is, what it provides, and how to use it. Update it only if the changes in this branch affect something a new user or reader of the project would need to know. Good candidates: new public modules or entry points, changed installation/setup steps, a new capability that alters what the project can do, a bump to the pinned game-engine-core dependency that changes what a consumer sees, removed or renamed entry points.

Do not update it for: internal refactors, bug fixes, test changes, performance tweaks, or implementation details that don't change the public surface. The README should grow slowly and deliberately — no change is a perfectly valid outcome.

After reviewing the diff, either apply the minimal edits needed or, if no update is warranted, briefly explain why and make no changes.
