"""The learned play engine: position encoding, network, and evaluator.

Everything in this subpackage imports `torch` (the `learning` extra) — import
it lazily, never from base-install code paths.
"""
