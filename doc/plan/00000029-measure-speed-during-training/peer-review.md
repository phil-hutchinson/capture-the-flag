# Peer Review — 00000029 Measure speed during training

## Summary

The branch adds a game-agnostic, call-path-nested cumulative timer
(`capture_the_flag/instrumentation/`), instruments every seam this repository
owns (position mechanics, the learned evaluator and its decode phases, the
network forward pass and mode switch, the boundary into `MCTSEngine`, and the
training orchestrator's generation split), and leaves each run a
`timings.json`/`timings.txt` pair carrying settings, environment, and the tree.
A seeded benchmark measures the instrumentation's own cost, and the resulting
"on by default" decision, the measurement recipe, and the findings from a
full-scale run are written up in the story folder. The design is careful and
unusually well documented, the region-per-call rule from the plan is respected
throughout, and coverage of the story's acceptance criteria is close to
complete.

Static checks from the repository root: **`pyright` — 0 errors, 0 warnings, 0
informations**; **`ruff check .` — all checks passed**. The default test suite
passes (313 passed, 15 deselected). The `slow` suite does **not**: running
`pytest -m slow tests/instrumentation` failed on two of three attempts (see
issue 1) — the failure is in the test, not in the production code, but the
branch as delivered has a red slow suite.

The implementation plan does include a README review step (Step 10), and the
README was updated accordingly, so no comment is raised on that.

## Comments

### Critical

| # | Status | Resolution | Location | Comment | Suggested Change | Code Snippet |
|---|--------|------------|----------|---------|-----------------|--------------|
| 1 | Fixed | `call_counts` now returns the flattened pairs sorted by path, so the comparison is sensitive to shape and counts but not to the report's cost ordering. `pytest -m slow tests/instrumentation` passed on three consecutive runs (previously 2 of 3 failed). | [tests/instrumentation/test_timing_is_transparent.py#L46-L57](../../../tests/instrumentation/test_timing_is_transparent.py#L46) | `call_counts` flattens the record depth-first and compares the resulting **list**, but the JSON's child order is the report's inclusive-time-descending sort (`report.py::_build`) — i.e. it is derived from wall clock, which the test's own docstring calls "noise-prone and never compared for equality". Two identically seeded runs with identical counts therefore fail whenever two sibling regions of similar cost swap places. Reproduced twice out of three runs of `pytest -m slow tests/instrumentation`: `AssertionError ... At index 6 diff: ('.../decode-policy/read-ply-probabilities', 483) != ('.../decode-policy/build-policy-mask', 483)` — same counts, different order. The consequence is that the story's "repeated seeded runs reproduce identical call counts" guarantee is not actually enforced (a genuine count regression would be indistinguishable from this noise), and a developer running the slow suite sees red. | Make the comparison order-independent while keeping shape sensitivity — sort the flattened pairs by path before returning, or compare `dict(...)` of path → calls. Ordering by cost is a property of the *report*, and if it is worth asserting it belongs in a separate, deliberately tolerant test. | `return sorted(walk(record["timings"], ""))` |

### Major

| # | Status | Resolution | Location | Comment | Suggested Change | Code Snippet |
|---|--------|------------|----------|---------|-----------------|--------------|
| 2 | Fixed | Resolved by writing the record at every checkpoint rather than by a `finally` — chosen deliberately, since a `finally` runs in an unknown state and does nothing for a hard kill. `TimingSession.snapshot()` (new) copies the tree crediting still-open regions their in-flight time, so a mid-run report reconciles like a finished one; `_run_generations` rewrites the pair after each `save_checkpoint`, silently (`echo=False`), and the final write replaces it. Verified end to end: a `SIGKILL`ed 40-generation run left a coherent record for the 5 generations it had finished, loss lines and all. | [capture_the_flag/engines/neural_network/ctf_training_run.py#L276-L318](../../../capture_the_flag/engines/neural_network/ctf_training_run.py#L276) | The record is written only on the success path: `_report_timings` sits *after* the `with timing_run(...)` block, so any exception — including the `KeyboardInterrupt` that ends most long runs a developer decides to cut short — discards the entire measurement. The 2026-07-24 reference run was 2h57m; losing three hours of accumulated regions to a Ctrl-C is a real cost, and the session is already closed and complete by the time the exception passes through `timing_session`'s `finally`. `run_batch` has the same shape. The story's wording ("when it finishes") arguably permits this, but nothing about the design requires it. | Write the record in a `finally`, guarded on the session existing, so an interrupted or failed run still leaves what it measured (optionally marking the record as incomplete via the existing `kind`/`settings` fields). Same treatment for `resume_generations` and `run_batch`. | `try:\n    with timing_run(...) as session: ...\nfinally:\n    _report_timings(session, run_dir, ...)` |

### Minor

| # | Status | Resolution | Location | Comment | Suggested Change | Code Snippet |
|---|--------|------------|----------|---------|-----------------|--------------|
| 3 | Open | | [capture_the_flag/instrumentation/report.py#L59-L68](../../../capture_the_flag/instrumentation/report.py#L59) | For a leaf region `unattributed_seconds` equals its whole inclusive time (no children to subtract), which is literally correct but reads as "unexplained". The console form suppresses the row for leaves (`_format_rows` returns early when there are no children), so the two output forms emphasise different things — and the JSON is the form `measurement-recipe.md` tells a future reader to compare mechanically, where summing `unattributed_seconds` across the tree would double-count nearly the whole run. | Either omit `unattributed_seconds` for childless nodes in `report_to_dict`, or document in the module docstring that the field is only meaningful where `children` is non-empty. | `"unattributed_seconds": round(report.unattributed_seconds, 6),` |
| 4 | Open | | [capture_the_flag/timing_regions.py#L44-L45](../../../capture_the_flag/timing_regions.py#L44) | The story lists "capturing the training sample" among the seams to instrument inside self-play. What exists is `policy-transform` (the capture-time re-framing) and `encode-position` reached from the collector; the capture itself is never named, so its non-transform cost lands in `self-play`'s remainder. The collector is upstream, so this is defensible — but neither the plan nor the docs say the item was consciously narrowed. | Say so explicitly (a line in `timing_regions.py` next to `POLICY_TRANSFORM`, or in `findings.md`), or add a region if any part of the capture path is reachable from this repository. | `POLICY_TRANSFORM = "policy-transform"` |
| 5 | Open | | [implementation-plan.md#L164-L176](implementation-plan.md#L164) | Story–plan discrepancy: the story's coverage list asks for "**Inside training** — assembling batches, forward, loss, backward and optimizer step". The plan explicitly declines three of the five (they sit inside the shared `TrainingLoop`) and the implementation follows the plan, so the story's acceptance line is met only in part. This is the right engineering call — `findings.md` shows training at 0.5% of the run — but the story was never amended, leaving a reader comparing story to code with an apparent gap. | Note the narrowing in the story (or in `findings.md`'s "what the split confirms" section) so the two documents agree on what was delivered. | `POLICY_LOSS`, `NETWORK_FORWARD` present; batch assembly / backward / optimizer step in `train`'s remainder |
| 6 | Open | | [capture_the_flag/instrumentation/timed_search.py#L45-L49](../../../capture_the_flag/instrumentation/timed_search.py#L45) | Plan deviation: Step 5 specifies "a timing wrapper that stands in front of a `GameEngine` ... and delegating unchanged"; the implementation subclasses `MCTSEngine` instead. The module docstring gives a good reason (the shared self-play collector asks for an `MCTSEngine` by name), so the code is right — but the plan still describes the abandoned approach, and the subclass is strictly more coupled to the pinned dependency than a protocol wrapper would be. | Update the plan's Step 5 wording to match what was built, so the two documents do not disagree about the shape of the shareable code. | `class TimedMCTSEngine[...](MCTSEngine[TPly, TPosition, TEvaluator]):` |
| 7 | Open | | [findings.md#L1-L10](findings.md#L1) | `findings.md` is a substantial deliverable — it is what the story means by "findings are written down" — but no step in the implementation plan produces it. Step 8 covers `measurement-recipe.md` only, and Steps 12–14 embed a partial summary in their preamble. A future reader reconstructing the work from the plan would not know the document exists. | Add a step (or extend Step 8) naming `findings.md` as its artifact. | — |
| 8 | Open | | [capture_the_flag/instrumentation/timing.py#L187-L198](../../../capture_the_flag/instrumentation/timing.py#L187) | `region()` — the module's most-used public entry point — returns `TimingSession \| _InactiveRegion`, exposing a private class in a public signature. Callers that want to annotate a variable holding the result have no public name to use. | Introduce a small public `Protocol` (or a public type alias, e.g. `Region = AbstractContextManager[object]`) as the declared return type; the two concrete classes can stay private. | `def region(name: str) -> TimingSession \| _InactiveRegion:` |
| 9 | Open | | [capture_the_flag/timing_benchmark.py#L86-L105](../../../capture_the_flag/timing_benchmark.py#L86) | The measured window wraps the whole of `run_batch`, which on the timed arm also builds the report, serialises the JSON, shells out to `git rev-parse`, and writes two files. Those are fixed per-run costs, not per-region costs, so the arm difference is not purely "what instrumenting costs" — yet `measurement-recipe.md` reconciles the difference entirely against region entries (`39,412 × 816 ns`). Below this machine's noise floor it changes no conclusion, but the accounting is not quite what the write-up claims. | Either start the stopwatch inside `_play_batch` (excluding report emission) or add a sentence to the recipe noting that the timed arm also pays the one-off report cost. | `started = time.perf_counter()\nrun_batch(...)` |
| 10 | Open | | [capture_the_flag/batch_runner.py#L112-L131](../../../capture_the_flag/batch_runner.py#L112) | A batch silently overwrites `timings.json`/`timings.txt` when the same `--output-dir` is reused — the same hazard that resumed training runs go to some length to avoid (`TIMING_RESUME_STEM_TEMPLATE`, "the original record must survive, since it is the baseline"). A developer keeping a baseline in `./games` loses it on the next batch without warning. Note the benchmark's `--record-dir` relies on the current overwrite behaviour, so any change must keep that path working. | At minimum document the behaviour in the `--timing` help text; better, refuse to clobber (or suffix) an existing record when the directory already holds one. | `report_timings(session, directory=output_dir, kind=ROOT_BATCH, ...)` |
| 11 | Open | | [capture_the_flag/instrumentation/timing.py#L226-L230](../../../capture_the_flag/instrumentation/timing.py#L226) | `timing_session` raises when a session is already active, and `timing_run` inherits that. Since both entry points default to `timing=True`, calling `run_batch` from inside anything that already opened a session (a script instrumenting several batches, a future orchestrator) aborts the work rather than the measurement. The strictness is right for the "one run, one root" invariant, but the failure mode is disproportionate. | Consider having `timing_run` reuse an already-active session (recording under a nested region and skipping the record write) instead of raising; keep the hard error on `timing_session` itself. | `raise RuntimeError(f"a timing session ({...!r}) is already active")` |
| 12 | Open | | [capture_the_flag/run_environment.py#L120-L134](../../../capture_the_flag/run_environment.py#L120) | `_import_torch` and `_import_torch_module` carry no return annotation, so both infer as `Any` and silently defeat type checking for every `torch.*` access in this module (the rest of the file is fully annotated). | Annotate as `ModuleType \| None` (`from types import ModuleType`). | `def _import_torch():` |
