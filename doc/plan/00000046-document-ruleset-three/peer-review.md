# Peer Review — Document Ruleset Three (Story 00000046)

## Summary

This branch is documentation only: it adds five shadow documents under
`doc/ruleset/proposed-3/` (`README.md`, `rules.md`, `start-position.md`,
`technical-notes.md`, `changelog.md`) describing a proposed major 3 ruleset, plus
the story and implementation plan. No published document is touched, no code
changes, and the diff is additions only — consistent with the story's
documentation-only framing and with the plan's seven steps, all of which appear
to have been carried out (including the required README check at Step 7, whose
"no change needed" conclusion holds: nothing in the root `README.md` is falsified
by this branch).

`pyright` reports **0 errors, 0 warnings, 0 informations**; `ruff check .`
reports **All checks passed!** Neither tool has anything to say about a
docs-only branch, and neither produced a finding to file.

The quantitative content was independently re-derived and holds up:
1,345,344,000 / 2,690,688,000 / 672,672,000 arrangement counts, the 43.17% /
56.83% branch split over all 6,435 splits, zero disagreements between the
`S ≥ 22` threshold and the strongest-seven formulation, the 64-bit / 2⁵³
argument, and the `1112223F33444555` … `F555444333222111` bounds are all
correct. All internal links and heading anchors across the seven new files
resolve. The findings below are one incorrect derivation, one under-specified
encoding contract, one un-updated guidance file, and a set of smaller
consistency issues.

**Status: all 11 findings actioned on this branch**, each as the suggested change
in its row. The Location links point at the reviewed state, so line numbers have
shifted since.

## Comments

### Major

| # | Status | Resolution | Location | Comment | Suggested Change | Code Snippet |
|---|--------|------------|----------|---------|-----------------|--------------|
| 1 | Resolved | Fixed. The *diagonally behind* row now derives the same out-of-contact conclusion the row above it does — distance holds — with the tempo cost of lining up as the real asymmetry; the *directly beside* row states the lockstep both pieces end up in; and the conclusion no longer claims a pursuer can close from behind. | [doc/ruleset/proposed-3/technical-notes.md#L244](../../ruleset/proposed-3/technical-notes.md#L244) | The "diagonally behind" row of the pursuit table does not follow from the encumbrance rule the document states. Encumbrance is judged only from the moving piece's own eight neighbours *at the start of its ply* (`rules.md` §4.2, restated at L253–258 of this same file). Once the quarry has advanced two squares it is no longer adjacent to the pursuer, so a diagonally-behind pursuer is unencumbered on its own ply for exactly the reason the "directly behind" row gives one line above — it is "now out of contact." Worked example: pursuer A4, quarry B5, both heading north. B5's northward encumbrance squares are B6/A6/C6/A5/C5; A4 is behind, so the quarry advances to B7. The pursuer at A4 now has no adjacent enemy, is unencumbered, and advances to A6. Chebyshev distance was 1 and is still 1 — **the quarry does not gain**. The section's conclusion inherits the error: the closing sentence at L249–251 says a pursuer must "get directly behind it or alongside it" to close, but the table's own first row says directly behind only holds distance, and alongside likewise only holds it (the beside-pursuer is itself encumbered on the following ply). | Correct the row to match the rule, or state the property that is actually true. The real asymmetry appears to be that a diagonally-behind pursuer can never convert to directly-behind without spending a tempo, not that it is encumbered. Rewrite the conclusion to match whatever the corrected table says — as written it asserts closing is possible from positions the table says it is not. | `\| diagonally behind \| the quarry advances two; the pursuer is encumbered and manages one. **The quarry gains.** \|` |
| 2 | Resolved | Fixed. Case is now normalised rather than tolerated: a reader must upper-case on input, and comparison, sorting and storage are defined on the upper-cased form. The identity sentence and the sorting claim were updated to match. | [doc/ruleset/proposed-3/start-position.md#L233](../../ruleset/proposed-3/start-position.md#L233) | The position-ID contract is self-contradictory on case. L233 says a reader "should accept either case", but L237–239 defines position identity as string equality (`two positions are the same when their codes match as strings`) and L251–253 promises that codes "sort as strings in the same order they would as numbers." Both of those hold only for a single case: `'F'` is `0x46` and `'f'` is `0x66`, so `…f…` sorts after every digit *and* after `A`–`E`, and a lowercase-emitting implementation's codes never compare equal to an uppercase one's. Two conforming implementations can therefore disagree about whether two IDs name the same position. | State a normalisation rule rather than a tolerance: a reader must upper-case an ID on input, and all comparison, sorting and storage are defined on the upper-cased form. | `- **Write uppercase**; a reader should accept either case.` |
| 3 | Resolved | Fixed. `doc/ruleset/CLAUDE.md` gains a **Shadow folders: whole-ruleset proposals** section scoping authority, the changelog rule and outbound dependencies to the folder, and the authoritative-ruleset sentence now says *at this level*. | [doc/ruleset/CLAUDE.md#L3](../../ruleset/CLAUDE.md#L3) | `doc/ruleset/CLAUDE.md` governs this exact folder and was not updated for the new `proposed-3/` subfolder, which neither the story nor the plan covers. As it stands it tells a future session that "`rules.md` in this folder is the **official, authoritative ruleset**" — now ambiguous with `proposed-3/rules.md` present — and that "Proposed-but-unpublished variants stay out of `rules.md` entirely — they belong in `proposed-variants.md`" (L20–22), which no longer describes where proposals live. The changelog rule ("Any change to `rules.md` must be accompanied by an entry in `changelog.md`") is also silent on whether `proposed-3/rules.md` and `proposed-3/changelog.md` are in or out of its scope. | Add a short section to `doc/ruleset/CLAUDE.md` stating that `proposed-3/` is a shadow folder carrying no authority, that the published/authoritative documents are the ones at this level, that the changelog rule does not apply within it, and how a whole-ruleset proposal differs from a single-flag entry in `proposed-variants.md`. | ``[`rules.md`](rules.md) in this folder is the **official, authoritative ruleset**`` |

### Minor

| # | Status | Resolution | Location | Comment | Suggested Change | Code Snippet |
|---|--------|------------|----------|---------|-----------------|--------------|
| 4 | Resolved | Fixed. Now reads "Each half is four columns wide, so each player has 8 of their 16 home squares in each half." | [doc/ruleset/proposed-3/rules.md#L59-L60](../../ruleset/proposed-3/rules.md#L59-L60) | "Each half is 8 squares" is false as written — a half of an 8 × 8 board is 32 squares. The number 8 is the count of *home* squares a player has in each half, which is the quantity §3's strength rule actually depends on. The correct reading is recoverable from §3 ("The Flag's half always holds exactly 7 numbered pieces"), but the rulebook should not require the reader to reverse-engineer it. `start-position.md` L86–88 states it correctly ("Each half holds 8 of White's 16 squares"). | Say what is being halved: "Each half is four columns wide, so each player has 8 of their 16 home squares in each half." | `Each half is 8 squares.` |
| 5 | Resolved | Fixed. Restated in numbers: "A rank 5 that wins a fight becomes a rank 4." Piece names now appear only in the §2.2 roster table. | [doc/ruleset/proposed-3/rules.md#L230](../../ruleset/proposed-3/rules.md#L230) | The rank-reduction illustration is the one place in `rules.md` that explains a rule using piece *names*, which cuts against the document's own claim eight lines earlier that "The number is the rule; the name is decoration" (L81), against `technical-notes.md` L75–78 ("nothing — not the notation, not the position block, not any rule — refers to a piece by name"), and against the plan's Step 2 verification criterion ("no rule anywhere refers to a piece by name"). It is also the hardest sentence for a major 2 reader to parse, since both names exist at major 2 at different ranks. | Restate in numbers: "A rank 5 that wins a fight becomes a rank 4: in every respect and for the rest of the game." | `A Master-of-Arms that wins a fight becomes a Champion: rank 4, in every respect` |
| 6 | Resolved | Fixed. The two conventions are split and each given its own reason — fixed width for string comparison and sorting, with the `0` case flagged as forward-looking to Reserved digits, and case normalisation per #2. | [doc/ruleset/proposed-3/start-position.md#L229-L233](../../ruleset/proposed-3/start-position.md#L229-L233) | "Two conventions, both because `0` is a meaningful digit" does not hold for either convention as stated. The uppercase rule has nothing to do with `0`. And at major 3 a valid code can never contain `0` at all — every home square is occupied, and the validation rule at L272–274 requires exactly three each of `1`–`5` and one `F` — so "zero-padded" and "a leading `0` is significant" describe a case that cannot arise in this ruleset; the fixed width matters here for string sorting, and the `0` rationale is forward-looking to the reserved-digit extension at L255–262. | Split the two: give fixed width its actual major 3 justification (string comparison and sorting), and note that `0` is reserved for a future ruleset whose home area is not filled, cross-referencing the Reserved digits section. Give the uppercase rule its own reason (see #2). | ``Two conventions, both because `0` is a meaningful digit:`` |
| 7 | Resolved | Fixed. Replaced with the full old→new table for all seven names, plus the two distinct failure modes: name→artwork survives the first two rows, name→digit fails on all four. | [doc/ruleset/proposed-3/changelog.md#L28-L31](../../ruleset/proposed-3/changelog.md#L28-L31) | The "two silent breakages" section gives `Foot Soldier` as its only example of a reused name, but `Militia` is reused just as silently and moves further: it is rank **6** in `2-0:BATTLE` — the weakest piece in that army — and rank **2** here, the second weakest of five. `Master-of-Arms` and `Champion` also both persist while their digits invert (1→5, 2→4), which is the case most likely to lull a consumer into assuming the name mapping is stable. This is the entry's headline warning, so the example set should be complete. | Give the full old→new table for the four carried-over names, or state explicitly that every major 2 name that survives into major 3 carries a different digit. | ``**Piece names are reused at different ranks.** `Foot Soldier` is rank 5 in `2-0:BATTLE` and rank **3** in `3-0:PRE-RELEASE`.`` |
| 8 | Resolved | Fixed. The final clause now runs on the one-Flag budget — the boundary must be covered by friendly *non-numbered* pieces, there is exactly one, and the 8 × 8 grid has no cut square, so any non-empty proper subset has a boundary of at least two. | [doc/ruleset/proposed-3/technical-notes.md#L350](../../ruleset/proposed-3/technical-notes.md#L350) | The boxed-in impossibility argument is presented as a derivation (and the plan's Step 4 asks that it hold up as one), but the final clause does not carry it. "A set of at most 16 squares cannot [enclose itself] on a 64-square board" is not self-evident — 16 squares can certainly enclose a region — and it omits the fact the argument actually turns on: the enclosing squares must be *friendly and non-numbered*, and there is exactly one such piece, the Flag. Since every numbered piece must be stuck simultaneously, the set of numbered pieces would need its entire on-board boundary covered by that single Flag square, and any non-empty proper subset of the grid has a boundary of at least two squares. | Replace the final clause with the one-Flag budget argument, which is short and airtight. | `> alone, which a set of at most 16 squares cannot do on a 64-square board.` |
| 9 | Resolved | Fixed. `proposed-3/technical-notes.md` gains **The header tag roster gains `StartPosition`**, recording the tag as optional and stating the reader's obligation to ignore unrecognised tags. | [doc/ruleset/proposed-3/start-position.md#L281-L283](../../ruleset/proposed-3/start-position.md#L281-L283) | `[StartPosition "…"]` is introduced as a new header tag by both `start-position.md` and `changelog.md` L82–89, but the record format it extends lives in the *published* `technical-notes.md` (L456–459), which defines a closed roster — "PGN's Seven Tag Roster plus `ResultReason` and `Ruleset`" — and says nothing about unknown tags. No shadow document places `StartPosition` into that roster or states that a reader must ignore tags it does not recognise, so an implementer working from the published record spec finds a roster that excludes it. | Add a line to `proposed-3/technical-notes.md` under "The notation" recording that major 3 extends the tag roster with `StartPosition` (optional, ignorable), and state the reader's obligation for unknown tags. | `The ID is carried in an **optional** header tag on a game record.` |
| 10 | Resolved | Fixed. `proposed-3/technical-notes.md` gains **The two diagonal-attack proposals are absorbed, not graduated** under *No rule settings at launch*, recording that both are baseline here and that their major 2 entries are unaffected. | [doc/ruleset/proposed-3/technical-notes.md#L262-L281](../../ruleset/proposed-3/technical-notes.md#L262-L281) | The story records a decision that none of the five durable documents does: that `DIAGONAL_ATTACKABLE` and `DIAGONAL_ATTACK_PATH` from `proposed-variants.md` are adopted as **baseline** at major 3 rather than graduated as flags, and that their entries remain live proposals against major 2 (story.md L96–100). No shadow document mentions `proposed-variants.md` at all, and `proposed-variants.md` has no pointer forward, so a reader of either file cannot tell that the two overlap. Since "No rule settings at launch" (L418–432) already enumerates the settings that were considered and rejected, this is the natural place for it. | Add a short paragraph to `proposed-3/technical-notes.md` recording the relationship, and — if major 3 is adopted — a note in `proposed-variants.md` pointing at it. | `## No rule settings at launch` |
| 11 | Resolved | Fixed. Now reads "all five documents in `proposed-3/`". | [implementation-plan.md#L120](implementation-plan.md#L120) | Step 6's verification says "a link-resolution script over all six new files"; there are five documents in `proposed-3/`. If the sixth was meant to be the published `doc/ruleset/CLAUDE.md` or `README.md`, the plan does not say so — and no such update was made (see #3). | Correct the count to five, or name the sixth file if one was intended. | `Verification (improvised): a link-resolution script over all six new files, plus a` |
